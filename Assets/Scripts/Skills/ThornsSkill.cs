using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = nameof(ThornsSkill) + "Config", menuName = "Skills/" + nameof(ThornsSkill), order = 0)]
public class ThornsSkill : SkillConfig
{
    
}
